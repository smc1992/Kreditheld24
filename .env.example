# WordPress Headless CMS Configuration
# Copy this file to .env.local and fill in your WordPress details

# WordPress Site URL (without trailing slash)
# In production this will be your subdomain: https://cms.kreditheld24.de
WORDPRESS_URL=http://localhost:8080
NEXT_PUBLIC_WORDPRESS_URL=http://localhost:8080

# WordPress Hostname (for CORS and security)
WORDPRESS_HOSTNAME=localhost:8080

# WordPress Webhook Secret (for revalidation)
# Generate a random string for security
WORDPRESS_WEBHOOK_SECRET=your-secret-key-here

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3001
VERIFICATION_BASE_URL=http://localhost:3001

# Email Configuration (for contact forms and notifications)
# SMTP Settings for sending emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=info@kreditheld24.de
SMTP_PASS=your-email-password-here
EMAIL_PASSWORD=your-email-password-here

# Optional: WordPress Authentication (if using private content)
# WORDPRESS_AUTH_USERNAME=your-username
# WORDPRESS_AUTH_PASSWORD=your-app-password

# Optional: WordPress Application Password (for authenticated requests)
# WORDPRESS_APPLICATION_PASSWORD=your-application-password

# Revalidation Settings
REVALIDATE_SECRET=your-revalidate-secret

# Analytics (optional)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Email Configuration (for contact forms)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Redis (optional, for verification token store via Coolify)
# When deploying with Coolify and a Redis service, set:
# REDIS_URL=redis://redis:6379
# REDIS_PASSWORD=your-redis-password


# Europace API Credentials (Client-Credentials OAuth)
# Füllen Sie diese in Ihrer .env.local mit echten Werten
EUROPACE_CLIENT_ID=
EUROPACE_CLIENT_SECRET=
# Optional: Überschreiben des OAuth-Token-Endpunkts
EUROPACE_OAUTH_URL=https://oauth-api.europace.de/oauth/token
# Optional: Organisations-/Personen-IDs aus EP2
EUROPACE_ORG_ID=SFH70
EUROPACE_PERSON_ID=XPS71
